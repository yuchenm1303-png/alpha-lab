"""Market-data provider adapters.

Provider-specific clients belong here. The analysis engine only consumes normalized
rows stored in DuckDB, so upstream sources can be added or replaced independently.
"""
